package com.example.spotifx2.controllers;

import javafx.fxml.FXML;
import javafx.scene.control.Slider;
import javafx.scene.control.ComboBox;
import javafx.scene.control.CheckBox;

public class SettingsController {
    /*
    @FXML private Slider volumeSlider;
    @FXML private ComboBox<String> qualityComboBox;
    @FXML private CheckBox shuffleCheckBox;
    @FXML private CheckBox repeatCheckBox;

    @FXML
    private void initialize() {
        // Initialize quality options
        qualityComboBox.getItems().addAll("High (320 kbps)", "Medium (192 kbps)", "Low (128 kbps)");
        qualityComboBox.setValue("High (320 kbps)");

    }

    @FXML
    private void saveSettings() {
        // Save settings logic
        System.out.println("Settings saved:");
        System.out.println("Volume: " + volumeSlider.getValue());
        System.out.println("Quality: " + qualityComboBox.getValue());
    }

     */
}
